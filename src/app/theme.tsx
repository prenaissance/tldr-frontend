import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#b3d4fc",
    },
    secondary: {
      main: "#003785",
    },
  },
});

export default theme;
